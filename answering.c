/*Ответы на вопросы по 0а.*/File main.cpp:#include "module1.h"#include "module2.h"#include <iostream>int main(int argc, char** argv){    std::cout << "Hello world!" << "\n";    std::cout << Module1::getMyName() << "\n";    std::cout << Module2::getMyName() << "\n";    using namespace Module1;    std::cout << getMyName() << "\n"; // (A)    std::cout << Module2::getMyName() << "\n";//using namespace Module2; // (B)//std::cout << getMyName() << "\n"; // COMPILATION ERROR (C)    using Module2::getMyName;    std::cout << getMyName() << "\n"; // (D)}1. Собрать программу и убедиться, что на каждый *.cpp файл создается отдельный объектный файл с тем же именем (для Visual Studio, например, в папке Debug будут создаваться файлы с расширением *.obj). - В Clion обьектные файлы создаются в lab0a/cmake-build-debug/CMakeFiles/lab0.dir2. Объяснить, что выведется при выполнении строк с комментариями (А) и (D) в main.cpp- В строке (А) вызовется функция getMyName пространства имен Module1. В строке (D) -  getMyName пространства имен Module2.3. Убедиться, что раскомментирование строк (B) и (C) в main.cpp приводит к ошибке компиляции. Объяснить, почему эта ошибка происходит, и предложить пути её устранения.- Oшибка происходит из за того, что подключены два пространства имен, и оба имеют функцию с одинаковым названием. Для устранения ошибки вызовем функцию через оператор разрешения области видимости (::) и ошибка пропадает.4. Объяснить, как можно избавиться от необходимости писать std::cout и вместо этого писать просто cout.-  Using namespace std.